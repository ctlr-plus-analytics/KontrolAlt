envVarGroups:
  - name: scraper-secrets
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: SERP_API_KEY
        sync: false
      - key: PROXY_LIST
        sync: false
      - key: GOOGLE_API_KEY
        sync: false

services:
  # ── Redis ──────────────────────────────────────────────────────────────────
  - type: redis
    name: redis
    plan: starter
    maxmemoryPolicy: noeviction
    ipAllowList: []

  # ── Backend API ────────────────────────────────────────────────────────────
  - type: web
    name: backend
    env: docker
    plan: starter
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: FRONTEND_ORIGIN
        sync: false
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: INFO

  # ── Celery Beat ────────────────────────────────────────────────────────────
  - type: worker
    name: beat
    env: docker
    plan: starter
    dockerfilePath: ./scraper/Dockerfile
    dockerContext: .
    dockerCommand: /opt/venv/bin/celery -A worker.celery_app beat --loglevel=info --schedule=/tmp/celerybeat-schedule
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: PYTHONPATH
        value: /app

  # ── Discovery / orchestration worker ──────────────────────────────────────
  - type: worker
    name: worker-discovery
    env: docker
    plan: starter
    dockerfilePath: ./scraper/Dockerfile
    dockerContext: .
    dockerCommand: /opt/venv/bin/celery -A worker.celery_app worker --loglevel=info --queues=discovery --concurrency=2 --prefetch-multiplier=1 --hostname=worker-discovery@%h
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: PYTHONPATH
        value: /app

  # ── Gate 0 worker ──────────────────────────────────────────────────────────
  - type: worker
    name: worker-gate0
    env: docker
    plan: starter
    dockerfilePath: ./scraper/Dockerfile
    dockerContext: .
    dockerCommand: /opt/venv/bin/celery -A worker.celery_app worker --loglevel=info --queues=gate0 --concurrency=2 --prefetch-multiplier=1 --hostname=worker-gate0@%h
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: PYTHONPATH
        value: /app

  # ── AI classification worker ───────────────────────────────────────────────
  - type: worker
    name: worker-classify
    env: docker
    plan: starter
    dockerfilePath: ./scraper/Dockerfile
    dockerContext: .
    dockerCommand: /opt/venv/bin/celery -A worker.celery_app worker --loglevel=info --queues=classify --concurrency=2 --prefetch-multiplier=1 --hostname=worker-classify@%h
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: PYTHONPATH
        value: /app

  # ── Rumble browser worker ──────────────────────────────────────────────────
  - type: worker
    name: worker-rumble
    env: docker
    plan: standard
    dockerfilePath: ./scraper/Dockerfile
    dockerContext: .
    dockerCommand: /opt/venv/bin/celery -A worker.celery_app worker --loglevel=info --queues=rumble --concurrency=2 --prefetch-multiplier=1 --hostname=worker-rumble@%h
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: PYTHONPATH
        value: /app
      - key: BROWSER_HEADLESS
        value: "true"

  # ── Substack browser worker ────────────────────────────────────────────────
  - type: worker
    name: worker-substack
    env: docker
    plan: standard
    dockerfilePath: ./scraper/Dockerfile
    dockerContext: .
    dockerCommand: /opt/venv/bin/celery -A worker.celery_app worker --loglevel=info --queues=substack --concurrency=2 --prefetch-multiplier=1 --hostname=worker-substack@%h
    envVars:
      - fromGroup: scraper-secrets
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis
          property: connectionString
      - key: PYTHONPATH
        value: /app
      - key: BROWSER_HEADLESS
        value: "true"
